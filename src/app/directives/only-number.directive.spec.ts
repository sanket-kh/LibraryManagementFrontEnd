import { NoAlphabetsDirective } from './no-alphabets.directive';

describe('OnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NoAlphabetsDirective();
    expect(directive).toBeTruthy();
  });
});
